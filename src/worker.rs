

struct Function{
    name: String,
    coroutine: String, //"WorkerCoroutine",
    timeout_s: f32,
    keep_result_s: f32,
    keep_result_forever: f32,
    max_tries: i32,
    
}
        //String | Function | "WorketCo.."
fn func(coroutine: Option<String>, name: Option<String>, keep_result: Option<>, timeout: Option<> ) {


}